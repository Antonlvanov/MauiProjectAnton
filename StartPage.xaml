<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiProjectAnton.StartPage"
             Title="Avaleht">
    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">
            <Button 
                Text="Tekst Page"
                Clicked="NavigateToTextPage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="Figure Page"
                Clicked="NavigateToFigurePage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="Timer Page"
                Clicked="NavigateToTimerPage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="Valgusfoor"
                Clicked="NavigateToValgusfoorPage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="DateTime Page"
                Clicked="NavigateToDateTimePage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="Värvid"
                Clicked="NavigateToColorPage"
                Style="{StaticResource NavButton}"/>
            
            <Button 
                Text="Lumememm"
                Clicked="NavigateToLumememmPage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="PickerImage"
                Clicked="NavigateToPickerImagePage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="PopUpPage"
                Clicked="NavigateToPopUpPage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="TripsTraps"
                Clicked="NavigateToTripsTrapsPage"
                Style="{StaticResource NavButton}"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>