<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiProjectAnton.StartPage"
             Title="Avaleht">
    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">
            <Button 
                Text="Tekst Page"
                Clicked="NavigateToTextPage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="Figure Page"
                Clicked="NavigateToFigurePage"
                Style="{StaticResource NavButton}"/>

            <Button 
                Text="Timer Page"
                Clicked="NavigateToTimerPage"
                Style="{StaticResource NavButton}"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>